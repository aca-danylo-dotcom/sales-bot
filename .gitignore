# Секреты и локальные данные
.env
*.db
*.db-journal
*.db-wal
*.db-shm
# резервные копии базы (shop.db.bak-20260728-215020 и подобные)
*.db.bak-*
*.bak

# фото товаров (лежат рядом с базой, в репозиторий не идут)
media/

# логи запусков
*.log

# Виртуальное окружение
.venv/
venv/

# Python
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.pytest_cache/

# Фронтенд: исходники в репозитории, сборка — нет. web/dist собирает Docker
# (см. Dockerfile), и держать её в git значило бы коммитить одно и то же
# приложение дважды — в виде кода и в виде минифицированной копии.
web/dist/
node_modules/
