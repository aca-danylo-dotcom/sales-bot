aiogram>=3.13,<4.0
aiosqlite>=0.20
APScheduler>=3.10,<4.0
python-dotenv>=1.0
openai>=1.50
tzdata>=2024.1   # база часовых поясов для ZoneInfo("Europe/Kiev") — на Windows обязательна
httpx>=0.27      # отчёт событий в Agent Stats (services/agent_stats.py)
aiohttp>=3.9     # веб-CRM, отдача фото и вебхук Instagram (приезжает и с aiogram, но нужен нам явно)
Jinja2>=3.1          # шаблоны страниц CRM
aiohttp-jinja2>=1.6  # связка Jinja2 с aiohttp
